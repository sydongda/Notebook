[Unit]
Description=<one damo Service>
After=syslog.target network.target

[Service]
WorkingDirectory=</path/to/work>
ExecStartPre=<your prepare command>
ExecStart=<run your app>

User=<user name>
Group=<user group>

[Install]
WantedBy=multi-user.target
